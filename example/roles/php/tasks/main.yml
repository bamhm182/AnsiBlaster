---
- name: Install PHP and the Apache PHP module
  ansible.builtin.apt:
    name:
      - php
      - libapache2-mod-php
      - php-mysql
    state: present
    update_cache: true

- name: Restart Apache so it picks up the PHP module
  ansible.builtin.service:
    name: apache2
    state: restarted
